import { BootState }             from './boot';
import { PreloadScene }     from './preload';
import { MainMenuScene }    from './main-menu-scene';
import { GameScene }        from './game-scene';

export default [BootState, PreloadScene, MainMenuScene, GameScene];
